---
// The separate Dashboard page was merged into My Library (purchases +
// account details in one place). Keep this route alive as a redirect so
// old bookmarks/links still work.
return Astro.redirect('/my-library', 301);
---
